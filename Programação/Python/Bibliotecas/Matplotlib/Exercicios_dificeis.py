# 1️⃣ Crie um gráfico de mapa de calor (heatmap) para visualizar uma matriz de correlação de um conjunto de dados fictício (use np.random.rand para gerar os dados).

# 2️⃣ Plote um gráfico de violino (violin plot) para comparar a distribuição de 5 conjuntos de dados aleatórios com diferentes médias e desvios padrão.

# 3️⃣ Crie um gráfico de radar (spider chart) para comparar as pontuações de 3 jogadores em 5 categorias diferentes (use dados fictícios).

# 4️⃣ Plote um gráfico de streamplot para visualizar um campo vetorial 2D, onde os vetores são definidos por funções de x e y (exemplo: u = -y, v = x).

# 5️⃣ Crie um gráfico 3D animado de uma superfície que muda ao longo do tempo (use a função FuncAnimation do matplotlib.animation).

# 6️⃣ Plote um gráfico de dispersão 3D com pontos coloridos por uma quarta dimensão (use np.random.rand para gerar os dados).

# 7️⃣ Crie um gráfico interativo com um slider que ajusta o número de bins de um histograma em tempo real (use matplotlib.widgets.Slider).

# 8️⃣ Implemente um gráfico de projeção geográfica (mapa) usando a biblioteca Basemap ou Cartopy, plotando pontos em coordenadas específicas (exemplo: cidades).